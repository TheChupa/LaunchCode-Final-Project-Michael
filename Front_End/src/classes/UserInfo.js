export default class UserInfo {
    constructor(id, user, social, financial, identity, aiResponse) {
        this.id = id;
        this.user = user;  
        this.social = social;  
        this.financial = financial; 
        this.identity = identity; 
        this.aiResponse = aiResponse; 
    }
}